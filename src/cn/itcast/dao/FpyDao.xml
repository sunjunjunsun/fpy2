<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.itcast.dao.FpyDao">

<select id="findTotalByTesterName"  resultType="int" parameterType="java.util.HashMap">
    SELECT count(*) from `tt_borders`
		<where>
			<if test="testerName!=null and testerName!=''">
				AND testerName = #{testerName}
			</if>
			<if test="boardType!=null and boardType!=''">
				AND boardType = #{boardType} 
			</if>
			<if test="dStart!=null and dStart!=''">
               <![CDATA[ and DATE_FORMAT(testTime,'%Y-%m-%d %H:%T:%s')>=DATE_FORMAT(#{dStart},'%Y-%m-%d %H:%T:%s')]]>
			</if>
			<if test="dEnd!=null and dEnd!=''">
               <![CDATA[ and DATE_FORMAT(testTime,'%Y-%m-%d %H:%T:%s')<=DATE_FORMAT(#{dEnd},'%Y-%m-%d %H:%T:%s')]]>
			</if>
		</where>
</select>

<select id="findTotalByTesterNameAndFailOrPass" resultType="int"
		parameterType="java.util.HashMap">
		SELECT count(*) from `tt_borders`
		<where>
			<if test="testerName!=null and testerName!=''">
				AND testerName = #{testerName}
			</if>
			<if test="boardType!=null and boardType!=''">
				AND boardType = #{boardType} 
			</if>
			<if test="testStatus!=null and testStatus!=''">
			   AND testStatus like concat('%',#{testStatus},'%')
			</if>
			<if test="dStart!=null and dStart!=''">
               <![CDATA[ and DATE_FORMAT(testTime,'%Y-%m-%d %H:%T:%s')>=DATE_FORMAT(#{dStart},'%Y-%m-%d %H:%T:%s')]]>
			</if>
			<if test="dEnd!=null and dEnd!=''">
               <![CDATA[ and DATE_FORMAT(testTime,'%Y-%m-%d %H:%T:%s')<=DATE_FORMAT(#{dEnd},'%Y-%m-%d %H:%T:%s')]]>
			</if>
		</where>
	</select>

</mapper>